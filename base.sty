\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{base}

\RequirePackage{graphicx}
\RequirePackage{mathtools}
\RequirePackage{amssymb}
\RequirePackage{amsthm}
\RequirePackage[skins, theorems, raster, listings]{tcolorbox}
\RequirePackage{tikz}
\RequirePackage[dvipsnames]{xcolor}
\RequirePackage{enumitem}
\RequirePackage{microtype}
\RequirePackage{hyperref}

% Enumitem Settings

\setlist{listparindent=\parindent, parsep = 0pt, itemsep = 0pt, topsep = 0pt, labelindent = 2em, labelsep = 0.5em}

\setlist[enumerate, 1]{label=(\arabic*)}

\newlist{subexercises}{enumerate}{2}
\setlist[subexercises, 1]{label=(\alph*)}
\setlist[subexercises, 2]{label=(\roman*)}

\RequirePackage{myfontcommands}
\RequirePackage{myalgebracommands}
\RequirePackage{mystatisticscommands}
\RequirePackage{myanalysiscommands}

% General Macros

\newcommand{\nzero}{\setminus\{0\}}
\newcommand{\mb}[1]{\mathbf{#1}}
\renewcommand{\qedsymbol}{$\blacksquare$}
\newcommand{\rightimp}{{\noindent$\boldsymbol{(\Rightarrow)}$}~}
\newcommand{\leftimp}{{\noindent$\boldsymbol{(\Leftarrow)}$}~}
\newcommand{\inv}{^{-1}}
\newcommand{\zp}{\zbb^+}
\newcommand{\dd}{\,\mathrm{d}}
\renewcommand{\emptyset}{\varnothing}
\renewcommand{\bar}[1]{\widebar{#1}\hspace{0.75pt}{}}
\renewcommand{\pmod}[1]{\mkern4mu(\operatorname{mod}\mkern3mu#1)}
\newcommand{\ep}{\varepsilon}
\newcommand{\wt}[1]{\widetilde{#1}}
\newcommand{\wh}[1]{\smash{\widehat{#1}}}
\DeclarePairedDelimiter{\abs}{\lvert}{\rvert}
\DeclarePairedDelimiter{\gen}{\langle}{\rangle}
\DeclarePairedDelimiter{\floor}{\lfloor}{\rfloor}
\DeclarePairedDelimiter{\set}{\{}{\}}
\DeclareMathOperator{\argmin}{\arg\,\min}
\DeclareMathOperator{\argmax}{\arg\,\max}
\DeclareMathOperator{\im}{im}

% Storing Old Phi Command

\let\oldphi\phi
\let\phi\varphi
\let\varphi\oldphi

\endinput